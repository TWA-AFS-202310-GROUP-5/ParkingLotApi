using ParkingLotApi.Dtos;

namespace ParkingLotApi.Services
{
    public class ParkingLotService
    {/*
        public async Task<ParkingLotDto?> CreateArticle(ParkingLotDto parkingLot)
        {
            if (await userRepository.GetByName(parkingLot.Name) == null)
            {
                await userRepository.Create(new ParkingLotDto(parkingLot.Name));
            }

            return await this.articleRepository.CreateArticle(parkingLot);
        }
        */

    }
}
